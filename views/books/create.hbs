<!DOCTYPE html>
<html lang="ru">
{{> header title="Добавить книгу" metaTitle="Добавить книгу — Читайка"}}

    <section class="section">
      <div class="section__header">
        <h2 class="section__title">Новая книга</h2>
      </div>

      <form class="card" action="/books" method="post" enctype="multipart/form-data" style="max-width: 600px; margin: 0 auto; display: flex; flex-direction: column; gap: 1rem;">
        
        <div class="field">
          <label class="field__label">Название книги</label>
          <input type="text" name="title" required placeholder="Например: Война и мир">
        </div>

        <div class="field">
          <label class="field__label">Автор</label>
          <input type="text" name="author" required placeholder="Например: Л.Н. Толстой">
        </div>

        <div class="field">
          <label class="field__label">Жанр</label>
          <input type="text" name="genre" required placeholder="Например: Роман">
        </div>

        <div class="field">
          <label class="field__label">Описание</label>
          <textarea name="description" rows="5" maxlength="400" placeholder="Краткое описание книги (максимум 400 символов)"></textarea>
        </div>

        <div class="field">
          <label class="field__label">Обложка (изображение)</label>
          <input type="file" name="cover" accept="image/*">
        </div>

        <div class="field">
          <label class="field__label">Текст книги (файл)</label>
          <input type="file" name="text" accept=".txt,.pdf,.epub,.fb2" required>
        </div>

        <button class="btn-ghost" type="submit" style="justify-content: center; text-align: center;">Сохранить книгу</button>
      </form>
    </section>
{{> footer}}
</html>
