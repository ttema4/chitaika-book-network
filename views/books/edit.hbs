<!DOCTYPE html>
<html lang="ru">
{{> header title="Редактировать книгу" metaTitle="Редактировать книгу"}}
    <section class="section">
      <div class="section__header">
        <h2 class="section__title">Редактирование: {{book.title}}</h2>
      </div>

      <form class="card" action="/books/{{book.id}}?_method=PATCH" method="post" enctype="multipart/form-data" style="max-width: 600px; margin: 0 auto; display: flex; flex-direction: column; gap: 1rem;">
        
        <div class="field">
          <label class="field__label">Название книги</label>
          <input type="text" name="title" value="{{book.title}}" required placeholder="Например: Война и мир">
        </div>

        <div class="field">
          <label class="field__label">Автор</label>
          <input type="text" name="author" value="{{book.author}}" required placeholder="Например: Л.Н. Толстой">
        </div>

        <div class="field">
          <label class="field__label">Жанр</label>
          <input type="text" name="genre" value="{{book.genre}}" required placeholder="Например: Роман">
        </div>

        <div class="field">
          <label class="field__label">Описание</label>
          <textarea name="description" rows="5" maxlength="400" placeholder="Краткое описание книги (максимум 400 символов)">{{book.description}}</textarea>
        </div>

        <div class="field">
          <label class="field__label">Новая обложка (необязательно)</label>
          <input type="file" name="cover" accept="image/*">
        </div>

        <div class="field">
          <label class="field__label">Новый текст книги (необязательно)</label>
          <input type="file" name="text" accept=".txt,.pdf,.epub,.fb2">
        </div>

        <button class="btn-ghost" type="submit" style="justify-content: center; text-align: center;">Сохранить изменения</button>
      </form>
    </section>
{{> footer}}
</html>
