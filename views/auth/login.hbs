<!DOCTYPE html>
<html lang="ru">
{{> header title="Вход" metaTitle="Вход — Читайка"}}

  <main class="main">
    <section class="section auth auth--signin" id="signin">
      <h2 class="section__title separator-bottom">Вход в аккаунт</h2>
      <p class="lead">Войдите, чтобы писать отзывы, следить за друзьями и продолжать чтение.</p>

      {{#if error}}
        <div class="bn-error" style="margin-bottom: 20px;">
            {{error}}
        </div>
      {{/if}}

      <form class="card" action="/login" method="post" autocomplete="on">
        <label class="field">
          <span class="field__label">Email</span>
          <input type="email" name="email" placeholder="you@example.com" required>
        </label>
        <label class="field">
          <span class="field__label">Пароль</span>
          <input type="password" name="password" minlength="6" required>
        </label>
        <button class="btn-ghost" type="submit">Войти</button>
        <a class="link" href="/register">Создать аккаунт</a>
      </form>
    </section>
  </main>
{{> footer}}
</html>
