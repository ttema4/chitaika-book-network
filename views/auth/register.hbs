<!DOCTYPE html>
<html lang="ru">
{{> header title="Регистрация" metaTitle="Регистрация — Читайка"}}

  <main class="main">
    <section class="section auth auth--signup" id="signup">
      <h2 class="section__title">Регистрация</h2>
      <p class="lead">Присоединяйтесь к Читайке, чтобы открывать новые книги.</p>

      {{#if error}}
        <div class="bn-error" style="margin-bottom: 20px;">
            {{error}}
        </div>
      {{/if}}

      <form class="card" action="/register" method="post" autocomplete="on">
        <label class="field">
          <span class="field__label">Имя пользователя</span>
          <input type="text" name="username" placeholder="Ваше имя" required>
        </label>
        <label class="field">
          <span class="field__label">Email</span>
          <input type="email" name="email" placeholder="you@example.com" required>
        </label>
        <label class="field">
          <span class="field__label">Пароль</span>
          <input type="password" name="password" placeholder="Придумайте пароль" minlength="6" required>
        </label>
        <button class="btn-ghost" type="submit">Создать аккаунт</button>
        <a class="link" href="/login">Уже есть аккаунт?</a>
      </form>
    </section>
  </main>
{{> footer}}
</html>
